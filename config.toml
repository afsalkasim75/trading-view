# -----------* Email Service *-----------
email_address = "afsaltrade@outlook.com"
login_password = "K3207197s+"
imap_server_address = "imap-mail.outlook.com"
imap_server_port = 993

# --------------* Webhook *--------------
webhook_urls = [
    "https://3commas.io/trade_signal/trading_view",
    ]

# ----------------* Log *----------------
# save log as a text file
log_save = true

# console log with color
log_color = true

# include the current time zone into the log
log_time_zone = false

# highlight the log with the same color with log label
log_full_color = false